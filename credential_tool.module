<?php

/**
 * Implements hook_theme().
 */
function credential_tool_theme($existing, $type, $theme, $path)
{
        return [
                'cred_template' => [
                        'variables' => ['cred_block' => NULL,],
                ],
        ];
}
